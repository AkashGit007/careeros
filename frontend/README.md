# Frontend (not yet scaffolded)

Per the project's priority order (`docs/PROJECT_CONTEXT.md`), core backend
functionality (auth, resume intelligence, job matching) comes before
frontend work. This directory will be scaffolded with Next.js + TypeScript
when that milestone starts — see `docs/NEXT_STEPS.md`.
