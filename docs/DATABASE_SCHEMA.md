# DATABASE_SCHEMA.md

No tables implemented yet.

## Planned entities (implement only as needed per milestone)

`User`, `Resume`, `ResumeVersion`, `Skill`, `UserSkill`, `Experience`,
`Education`, `Project`, `Job`, `JobSkill`, `JobMatch`, `Application`,
`Interview`, `InterviewQuestion`, `InterviewAnswer`, `Recommendation`,
`CareerGoal`, `LearningRecommendation`, `Conversation`, `Memory`, `Document`

Update this file with real table definitions (columns, types, relationships)
as each is implemented via an Alembic migration. Do not document a table
that doesn't exist in a migration yet.
