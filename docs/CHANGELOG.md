# CHANGELOG.md

## 2026-09-03 — Project initialization

- Initialized Git repository
- Created `docs/` project memory files (this framework)
- Created backend skeleton: FastAPI app with health check, config module,
  SQLAlchemy base/session wiring, empty layered folders
  (`api/`, `models/`, `schemas/`, `services/`, `repositories/`, `ai/`, `utils/`)
- Created `.gitignore`, `.env.example`, root `README.md`
- No features implemented yet — see `DEVELOPMENT_STATUS.md`
